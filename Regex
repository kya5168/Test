using System;
using System.Text.RegularExpressions;

class Program
{
    static void Main()
    {
        Regex regex = new Regex(@"\d+");
        Match match = regex.Match("Net 55 Pounds");
        if (match.Success)
        {
            Console.WriteLine(match.Value);
        }
    }
}

Output - 55
